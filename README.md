# Шахматы 531 группы НПК

## Тестирование

Для запуска всех тестов наберите команду:

    python -m unittest discover test

Для тестирования отдельного модуля (например, модуля pawn.py):

    python -m unittest test.test_pawn
